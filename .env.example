# FileMaker MCP Server Configuration
# Configure your FileMaker database connections using environment variables

# Global settings
FM_PROTOCOL=https
FM_API_VERSION=v1
FM_SSL_VERIFY=false
NODE_TLS_REJECT_UNAUTHORIZED=0

# Cache settings
CACHE_TTL=840        # Data cache TTL in seconds (14 minutes)
SESSION_TTL=780      # Session cache TTL in seconds (13 minutes)

# Database configurations
# Pattern: FM_SERVER_<IDENTIFIER>, FM_DATABASE_<IDENTIFIER>, etc.
# You can configure as many databases as needed

# Example Database 1 - with username/password authentication
FM_SERVER_MYDB1=your-filemaker-server.com
FM_DATABASE_MYDB1=MyDatabase
FM_ACCOUNT_MYDB1=your_username
FM_PASSWORD_MYDB1=your_password

# Example Database 2 - with API key authentication (OttoFMS)
FM_SERVER_MYDB2=your-filemaker-server.com
FM_DATABASE_MYDB2=MyOtherDatabase
FM_API_KEY_MYDB2=your-api-key-here

# Add more databases as needed following the same pattern:
# FM_SERVER_IDENTIFIER=server_url
# FM_DATABASE_IDENTIFIER=database_name
# FM_ACCOUNT_IDENTIFIER=username (if using username/password)
# FM_PASSWORD_IDENTIFIER=password (if using username/password)
# FM_API_KEY_IDENTIFIER=api_key (if using API key authentication)
